// ContentView.swift is replaced by RootView.swift + the Views/ folder.
// This file is kept only so the Xcode project compiles without removing a reference.
// It is intentionally empty — delete it once you've confirmed the build succeeds.

import SwiftUI
